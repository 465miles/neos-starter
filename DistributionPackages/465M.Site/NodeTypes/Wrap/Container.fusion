prototype(465M.Site:Wrap.Container) < prototype(Neos.Fusion:Component) {

    spacingTop = ${q(node).property('spacing-top')}
    spacingBottom = ${q(node).property('spacing-bottom')}
    isNested = ${q(node).parent().is('[!instanceof 465M.Site:Collection.Main]')}
    // isNested = ${Array.last(Array.pop(String.split(node.path, '/'))) != "main"}
    content = ${value}

    renderer = afx`
        <465M.Site:Component.Organism.Container {...props} />
    `
}
