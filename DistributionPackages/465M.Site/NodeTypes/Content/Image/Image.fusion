prototype(465M.Site:Content.Image) < prototype(Neos.Neos:ContentComponent) {

    imageSource = Sitegeist.Kaleidoscope:AssetImageSource {
        asset = ${q(node).property('image')}
    }

    renderer = afx`
        <465M.Site:Component.Atom.Image imageSource={props.imageSource} />
    `
}
