prototype(465M.Site:Content.Headline) < prototype(Neos.Neos:ContentComponent) {

    tagName = ${q(node).property('title-hierarchy')}
    fontSize = ${q(node).property('title-size')}
    spacingTop = ${q(node).property('spacing-top')}
    spacingBottom = ${q(node).property('spacing-bottom')}
    content = Neos.Neos:Editable {
        property = 'title'
    }

    renderer = afx`
        <465M.Site:Component.Molecule.Headline {...props} />
    `
}
